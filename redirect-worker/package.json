{
  "name": "@nexartis/homeport-sdk-legacy-redirect",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that 301-redirects the legacy nnn-sdk[-dev].nexartis.com hostnames to homeport-sdk[-dev].nexartis.com for the announced deprecation window.",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
