# Nexartis Infrastructure Standards
# @nexartis/* is published to public npmjs — installs are tokenless.
# CI publishing uses npmjs Trusted Publishing (OIDC + provenance); no
# long-lived NPM_TOKEN is required locally or in CI.

engine-strict=true
auto-install-peers=true
save-exact=true
