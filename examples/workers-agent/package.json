{
  "name": "@example/workers-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal Cloudflare Workers consumer of @nexartis/homeport-sdk.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod"
  },
  "dependencies": {
    "@nexartis/homeport-sdk": "^1.2.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260418.1",
    "wrangler": "^4.83.0"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
